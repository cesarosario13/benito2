<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Usuarios</title>
    <!-- Vincular el archivo CSS -->
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
    <div class="container">
        <h1>Lista de Usuarios</h1>
        <a href="/users/new" class="create-button">Crear nuevo usuario</a>

        <% if (usuarios.length > 0) { %>
            <ul class="user-list">
                <% usuarios.forEach(usuario => { %>
                    <li class="user-item">
                        <p><strong>Nombre:</strong> <%= usuario.nombre %></p>
                        <p><strong>Email:</strong> <%= usuario.email %></p>
                        <p><strong>Teléfono:</strong> <%= usuario.telefono || 'No proporcionado' %></p>
                        <p><strong>Dirección:</strong> <%= usuario.direccion || 'No proporcionada' %></p>
                        <p><strong>Categoría:</strong> <%= usuario.categoria %></p>
                        <p><strong>Proyecto:</strong> <%= usuario.proyecto || 'No asignado' %></p>
                        <div class="actions">
                            <a href="/users/<%= usuario._id %>/edit">Editar</a>
                            <form action="/users/<%= usuario._id %>/delete" method="POST" onsubmit="return confirm('¿Estás seguro de eliminar este usuario?');" style="display: inline;">
                                <button type="submit">Eliminar</button>
                            </form>
                        </div>
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <p class="no-users">No hay usuarios registrados.</p>
        <% } %>
    </div>
</body>
</html>